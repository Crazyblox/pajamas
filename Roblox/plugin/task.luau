--!strict
--!native

--[[
	Pajamas plugin - task:
	
	task is not a native library to Luau, so to keep a pajamas
	project platform-agnostic, we must resort to silly things like this.
	
	However, the task library is present in at least 2 Luau runtimes as of writing.
]]

return task
